import tkinter
root = tkinter.Tk()
theLabel = tkinter.Label(root, text="Hey this is the first program")   #to make a label in the window
theLabel.pack()    #to pack the label the window anywhere in window
root.mainloop()    #to keep the window running until we don't close it